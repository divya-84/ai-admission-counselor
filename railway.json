{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate --schema=backend/prisma/schema.prisma && npm run build -w shared && npm run build -w backend"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=backend/prisma/schema.prisma && npm run start -w backend",
    "restartPolicyType": "ON_FAILURE"
  }
}
