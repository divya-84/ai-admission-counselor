{
  "name": "ai-admission-counselor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AI Admission Counselor project containing frontend, backend, and shared modules.",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "npm run build -w shared && npx prisma db push --schema=backend/prisma/schema.prisma && npm run build -w backend && npm run build -w frontend",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "eslint": "^9.21.0",
    "globals": "^15.15.0",
    "husky": "^9.1.7",
    "prettier": "^3.5.1",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.24.1"
  }
}
